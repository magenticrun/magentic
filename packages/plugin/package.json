{
  "name": "@magentic/plugin",
  "version": "0.0.0",
  "private": true,
  "description": "The plugin contract: what a plugin can contribute to a magentic gateway.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun --bun vitest run --root ../.. packages/plugin",
    "catalog:sync": "bun scripts/sync-catalog.ts"
  },
  "dependencies": {
    "@magentic/protocol": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/platform-bun": "catalog:"
  }
}
